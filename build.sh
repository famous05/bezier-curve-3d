# From the project root (where your top-level CMakeLists.txt is)
mkdir build
cd build

# Configure the project: Tell CMake to generate build files here
cmake ..

# Build the project
cmake --build .

cd ../
